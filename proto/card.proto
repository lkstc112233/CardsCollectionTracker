syntax = "proto3";

package card_collection;

// Info for card info.
message CardInfo {
    // Id of the card
    string id = 1;
    // Card name
    string name = 2;
    // Image uri
    string image_uri = 3;
}